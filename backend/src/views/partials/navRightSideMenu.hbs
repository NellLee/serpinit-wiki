

    <div id="quick-links">
        <h3>Hier erwähnt:</h3>
        <nav>
            <ul>
                {{#each refContent}}
                <li>
                    <a href="{{this.href}}">
                        {{{this.text}}}
                    </a>
                </li>
                {{/each}}
            </ul>
        </nav>

        <h3>Verwandt:</h3>
        <nav>
            <ul>
                {{#each subContent}}
                <li>
                    <a href="{{this.href}}">
                        {{{this.text}}}
                    </a>
                </li>
                {{/each}}
            </ul>
        </nav>
    </div>
